<div class="kicker">Sync or async - the correct pattern exists</div>

# One blocking call stalls every request behind it

<div class="mt-6">

````md magic-move {at:2}
```python
@get("/reports/monthly")
async def monthly_report() -> dict[str, int]:
    # looks async - but there is no async pandas
    df = pd.read_csv("orders.csv")
    return {"orders": len(df)}
```

```python
@get("/reports/monthly", sync_to_thread=True)
def monthly_report() -> dict[str, int]:
    # same work - declared, so it runs in a worker thread
    df = pd.read_csv("orders.csv")
    return {"orders": len(df)}
```
````

</div>

<EventLoop v-click="1" :fixed="$clicks >= 2" class="mt-6 mb-2" />

<p class="text-lg mt-4" v-click="3">
  <span class="gold font-extrabold">Litestar won't guess:</span> an undeclared sync handler is a <strong>startup warning</strong>, not a production mystery.
</p>

<!--
Beat 1 (on entry): the handler looks fine — it's async, it returns JSON. Let
the room read it. The trap: pandas has no async API, so this parks the loop.

[click] The consequence: every request queued behind that read_csv waits.

[click] The fix morphs in — watch the two tokens: async def → def, plus
sync_to_thread=True. Same work, worker thread, loop keeps moving (diagram
unblocks).

[click] The design point: Litestar refuses to guess. A sync handler that
doesn't declare sync_to_thread is a warning at startup — the decision is
always explicit, never implicit.
-->
