<div class="kicker">Building blocks</div>

# A place for everything

<div class="concern-map mt-5">
  <div class="map-head">Concern</div><div class="map-head">Litestar construct</div>
  <template v-for="row in [
    ['Resource and route boundaries', 'Controllers and routers'],
    ['Shared resources', 'DI and lifespan'],
    ['Authorization', 'Guards'],
    ['Cross-cutting request behavior', 'Layered middleware'],
    ['External integrations', 'Plugins'],
  ]" :key="row[0]">
    <div v-click class="concern muted">{{ row[0] }}</div>
    <div v-after class="construct">{{ row[1] }}</div>
  </template>
</div>

<style scoped>
.concern-map { display: grid; grid-template-columns: 1.1fr 1fr; border: 1px solid var(--ls-line); border-radius: 14px; overflow: hidden; }
.concern-map > div { padding: .55rem 1rem; border-bottom: 1px solid var(--ls-line); }
.concern-map > div:nth-last-child(-n + 2) { border-bottom: 0; }
.map-head { background: var(--ls-bg-3); color: var(--ls-gold); font-size: .75rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
.construct { font-weight: 750; border-left: 1px solid var(--ls-line); }
</style>

<!-- Julien · 1:10 -->
