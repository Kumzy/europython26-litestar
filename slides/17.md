<div class="kicker">Lifecycles</div>

# Match resources and work to real lifetimes

<div class="mt-6 flex flex-col gap-4">
  <div v-click class="card flex items-center gap-6">
    <div class="gold font-extrabold text-xl" style="min-width:11rem">App scope</div>
    <div class="flex-1">
      connection pools, HTTP clients, config
      <div class="font-mono text-sm muted mt-1">lifespan / plugin startup → app.state</div>
    </div>
    <div class="muted italic text-sm" style="min-width:10rem">startup → shutdown</div>
  </div>
  <div v-click class="card flex items-center gap-6">
    <div class="gold font-extrabold text-xl" style="min-width:11rem">Request scope</div>
    <div class="flex-1">
      DB session, current user, request context
      <div class="font-mono text-sm muted mt-1">Provide(provider, use_cache=True)</div>
    </div>
    <div class="muted italic text-sm" style="min-width:10rem">request resolution</div>
  </div>
  <div v-click class="card flex items-center gap-6">
    <div class="gold font-extrabold text-xl" style="min-width:11rem">Background work</div>
    <div class="flex-1">
      order processing, email, durable jobs
      <div class="font-mono text-sm muted mt-1">Litestar Queues / SAQ</div>
    </div>
    <div class="muted italic text-sm" style="min-width:10rem">after the response</div>
  </div>
</div>

<p class="muted text-sm mt-4"><code>use_cache=True</code> does not create an application singleton; it avoids duplicate resolution during one request.</p>

<!-- Cody · end of architecture section at ~14:00 -->
