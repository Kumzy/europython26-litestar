---
layout: two-cols-header
layoutClass: gap-x-8
---

<div class="kicker">The classic leak</div>

# One blocking call stalls every request behind it

<EventLoop class="mt-3 mb-4" />

::left::

<div class="muted text-sm mb-1">❌ <em>looks</em> async — blocks the loop</div>

<<< @/examples/src/blocking.py#blocking python

::right::

<div v-click>
<div class="muted text-sm mb-1">✅ explicit — runs in the thread pool</div>

<<< @/examples/src/blocking.py#fixed python

<p class="gold italic text-sm mt-3">Litestar won't guess: a sync handler without <code>sync_to_thread</code> is a startup warning — the choice is always explicit.</p>

</div>

<!--
Every async framework has this failure mode — one sync call in an async handler
and throughput falls off a cliff, silently. The design answer: Litestar refuses
to guess. A sync handler that doesn't declare sync_to_thread gets a warning at
startup — the decision is always explicit, never implicit.
-->
