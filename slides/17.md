<div class="kicker">Lifecycles</div>

# Match scope to real lifetimes

<div class="mt-6 flex flex-col gap-4">
  <div v-click="1" class="card flex items-center gap-6">
    <div class="gold font-extrabold text-xl" style="min-width:11rem">App scope</div>
    <div class="flex-1">
      connection pools, HTTP clients, config
      <div class="font-mono text-sm muted mt-1">cli / lifespan / plugin startup → app.state</div>
    </div>
    <div class="muted italic text-sm" style="min-width:10rem">startup → shutdown</div>
  </div>
  <div v-click="1" class="card flex items-center gap-6">
    <div class="gold font-extrabold text-xl" style="min-width:11rem">Request scope</div>
    <div class="flex-1">
      DB session, current user, request context
      <div class="font-mono text-sm muted mt-1">"user": Provide(provide_current_user)</div>
    </div>
    <div class="muted italic text-sm" style="min-width:10rem">per request, torn down after</div>
  </div>
  <div v-click="1" class="card flex items-center gap-6">
    <div class="gold font-extrabold text-xl" style="min-width:11rem">Dependency scope</div>
    <div class="flex-1">
      derived values for one call
      <div class="font-mono text-sm muted mt-1">"filters": Provide(provide_filters)</div>
    </div>
    <div class="muted italic text-sm" style="min-width:10rem">as short-lived as the work</div>
  </div>
</div>

<!--
Recurring thread (second use): "Scope determines how often work runs" — app
scope runs once, request scope runs on every request.

Note: with Advanced Alchemy or SQLSpec you don't hand-wire DB sessions — the
plugin provides them at request scope. The example line stays deliberately
non-DB.

BRIDGE to 18 (Cody): "Correct scope prevents unnecessary work. The next
question is whether the work we do need can block everything else."
-->
