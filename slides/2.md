<div class="kicker">The real subject</div>

# Designing performant <span v-mark.strike-through>APIs</span> <span v-after class="gold">web apps</span>

<p class="lead">The proposal said APIs. Litestar now has explicit homes for a complete web application's recurring concerns.</p>

<div class="stack-grid mt-4">
  <StackThink />
  <div class="stack-arrow" v-click="1">→</div>
  <StackReality v-click="1" />
</div>

<style scoped>
.stack-grid {
  display: grid;
  grid-template-columns: 1fr auto 1.15fr;
  align-items: start;
  gap: 1.5rem;
}
.stack-arrow {
  align-self: center;
  font-size: 1.8rem;
  color: var(--ls-faint);
  padding-top: 2.2rem;
}
</style>

<!--
Julien · 0:45

The proposal said APIs. But Litestar has grown into a framework for complete
web applications. The HTTP handler is still small; production is everything
that must be coordinated around it.
-->
