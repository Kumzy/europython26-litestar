<div class="kicker">Measure the request path</div>

# Pair each structural change with evidence

<div class="measure-table mt-4">
  <div class="th">Pressure</div><div class="th">Structural move</div><div class="th">Measure</div>
  <template v-for="row in [
    ['Blocking call', 'async / thread / queue', 'event-loop lag · p95'],
    ['Repeated setup', 'lifespan / plugin', 'construction count'],
    ['Query growth', 'loading / pagination', 'query count · DB time'],
    ['Excess serialization', 'DTO', 'encoded bytes · encode time'],
    ['Long-running work', 'queue', 'request latency · queue depth'],
    ['Broad middleware', 'narrower layer', 'span time · invocation count'],
  ]" :key="row[0]">
    <div v-click class="pressure">{{ row[0] }}</div>
    <div v-after class="move gold">{{ row[1] }}</div>
    <div v-after class="metric muted">{{ row[2] }}</div>
  </template>
</div>

<style scoped>
.measure-table { display: grid; grid-template-columns: 1fr 1.05fr 1.25fr; border: 1px solid var(--ls-line); border-radius: 14px; overflow: hidden; }
.measure-table > div { padding: .52rem .75rem; border-bottom: 1px solid var(--ls-line); }
.measure-table > div:nth-child(3n + 2), .measure-table > div:nth-child(3n + 3) { border-left: 1px solid var(--ls-line); }
.measure-table > div:nth-last-child(-n + 3) { border-bottom: 0; }
.th { background: var(--ls-bg-3); color: var(--ls-gold); font-size: .72rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
.pressure, .move { font-weight: 700; }
.metric { font-size: .88rem; }
</style>

<!--
Cody · 1:45

Do not claim a universal speedup. State the intervention, choose the metric,
and compare before and after under representative load.
-->
