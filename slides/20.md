---
layout: two-cols-header
layoutClass: gap-x-10
---

<div class="kicker">Around Litestar</div>

# Batteries, when you want them

::left::

<p class="muted text-sm mb-1"><span class="gold font-bold">Advanced Alchemy</span> — slide 15's <code>repository.py</code> + <code>service.py</code>, already written:</p>

<<< @/examples/src/batteries.py#service python

::right::

<div class="flex flex-col gap-3 mt-1">
  <div class="card accent">
    <div class="gold font-extrabold mb-1">SQLSpec</div>
    <span class="text-sm">Type-safe, driver-agnostic SQL when an ORM is too much</span>
  </div>
  <div class="card accent">
    <div class="gold font-extrabold mb-1">Plugins</div>
    <span class="text-sm">Extend the app cleanly — DI, lifecycle hooks, OpenAPI, CLI</span>
  </div>
  <div class="card accent">
    <div class="gold font-extrabold mb-1">litestar-fullstack</div>
    <span class="text-sm">A production-shaped reference app to learn the patterns from</span>
  </div>
</div>

<!--
Keep this brief (~45s) — it's a "go explore" pointer, not a deep dive. The code
closes the loop with slide 15: the layers you structured by hand are the ones
Advanced Alchemy ships ready-made. Name the others, say why you'd reach for
each, move on.
-->
