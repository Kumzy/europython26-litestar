<div class="kicker">Where it leaks → how design fixes it</div>

# Five leaks, five fixes

<div class="leakfix mt-6">
  <div class="lf-row" v-click>
    <div class="leak"><span class="num-inline gold">1</span> <span class="muted">Work done on <em>every</em> request</span></div>
    <div class="arrow gold">→</div>
    <div class="fix">Scope it — build once per app, not per request</div>
  </div>
  <div class="lf-row" v-click>
    <div class="leak"><span class="num-inline gold">2</span> <span class="muted">A blocking call inside an async handler</span></div>
    <div class="arrow gold">→</div>
    <div class="fix">Async all I/O — or <code>sync_to_thread</code>, explicitly <span class="delta">one 2 s call = everyone waits 2 s</span></div>
  </div>
  <div class="lf-row" v-click>
    <div class="leak"><span class="num-inline gold">3</span> <span class="muted">Rebuilding heavy objects per request</span></div>
    <div class="arrow gold">→</div>
    <div class="fix">Match DI scope to real lifetime <span class="delta">a fresh HTTP client: ~8 ms/request → 0</span></div>
  </div>
  <div class="lf-row" v-click>
    <div class="leak"><span class="num-inline gold">4</span> <span class="muted">Serializing more than you return</span></div>
    <div class="arrow gold">→</div>
    <div class="fix">Tight DTOs — emit only the shape you declare <span class="delta">30 fields vs the 5 you need: ~5× encode cost</span></div>
  </div>
  <div class="lf-row" v-click>
    <div class="leak"><span class="num-inline gold">5</span> <span class="muted">Global middleware on every route</span></div>
    <div class="arrow gold">→</div>
    <div class="fix">Attach it to the layer that needs it — not the app</div>
  </div>
</div>

<p class="gold italic text-center mt-6" v-click>True in any framework — Litestar just makes the fix the default path.</p>

<p class="placeholder text-center">measured: httpx AsyncClient create/close, msgspec encode of 100 rows — this laptop; your numbers vary, the shape doesn't</p>

<style scoped>
.leakfix { display: flex; flex-direction: column; gap: .6rem; }
.lf-row { display: grid; grid-template-columns: 1.1fr 2rem 1.1fr; align-items: center; gap: 1rem; }
.num-inline { display: inline-block; min-width: 1.4rem; font-weight: 800; }
.arrow { font-size: 1.4rem; font-weight: 800; text-align: center; }
.fix { font-weight: 600; }
.delta { display: block; font-weight: 500; font-size: 0.72rem; color: var(--ls-mint); font-style: italic; }
</style>

<!--
This is the payoff slide and the takeaway in one: each way latency leaks, paired
with the design choice that prevents it. The right column holds true in any
framework — Litestar just makes each fix the path of least resistance.
-->
