---
layout: two-cols-header
layoutClass: gap-x-12
---

<div class="kicker" style="color:var(--ls-blue)">Low-level</div>

# Flask / Starlette - minimalist

::left::

```python
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.post("/orders")
def create_order():
    data = request.get_json()
    sku = data.get("sku")
    if not isinstance(sku, str) or not sku:
        return jsonify(error="invalid sku"), 400

    order = OrderService(db_session()).create(sku)
    return jsonify(id=order.id, sku=order.sku), 201
```

::right::

<div v-click>
<ul class="stars text-lg">
<li><strong>Maximum flexibility, minimal assumptions</strong></li>
<li class="muted">You rebuild the same plumbing on every project</li>
<li class="muted">Performance is on you — easy to get subtly wrong</li>
</ul>
</div>

<div class="mt-4" v-click>
<p class="mb-1 font-bold gold">Bring your own:</p>
<ul class="stars">
<li class="muted">Validation</li>
<li class="muted">Serialization</li>
<li class="muted">Dependency injection</li>
<li class="muted">OpenAPI</li>
</ul>
</div>
