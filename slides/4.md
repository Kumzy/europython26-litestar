---
layout: two-cols-header
layoutClass: gap-x-12
---

<div class="kicker">Day thirty</div>

# Then requirements arrive

::left::

````md magic-move
```python
@get("/orders")
async def list_orders() -> list[OrderDTO]:
    ...
```

```python
@get("/orders", guards=[require_user])
async def list_orders(
    user: User,  # from auth
    db: Session,  # injected
    page: int = 1,
) -> list[OrderDTO]:
    ...
```
````

::right::

<div v-click="1" class="h-full flex flex-col justify-center">
<ul class="stars text-lg">
<li>Auth, DB sessions, pagination, DTOs…</li>
<li class="muted">Each one is a dependency <em>and</em> a decision</li>
<li class="muted">Still fine — at 5 endpoints</li>
</ul>
</div>
