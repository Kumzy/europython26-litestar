<div class="kicker">Litestar's bet</div>

# High-level constructs with layered composition

<div class="layers mt-8">
  <div class="layer app" v-click>
    <div class="label">Application</div>
    <div class="sub">shared plugins · lifespan · defaults</div>
  </div>
  <div class="layer router" v-click>
    <div class="label">Router / controller</div>
    <div class="sub">domain boundaries · guards · middleware · DI</div>
  </div>
  <div class="layer handler" v-click>
    <div class="label">Handler</div>
    <div class="sub">the narrow request contract and domain coordination</div>
  </div>
</div>

<p class="text-center text-2xl font-bold mt-8" v-click>Put a concern at the narrowest layer that owns it.</p>

<style scoped>
.layers { display: flex; flex-direction: column; align-items: center; gap: .65rem; }
.layer { border: 1px solid var(--ls-line); border-radius: 13px; padding: .65rem 1.2rem; text-align: center; background: var(--ls-bg-2); }
.layer.app { width: 88%; border-color: color-mix(in srgb, var(--ls-violet) 65%, var(--ls-line)); }
.layer.router { width: 68%; border-color: color-mix(in srgb, var(--ls-blue) 65%, var(--ls-line)); }
.layer.handler { width: 48%; border-color: color-mix(in srgb, var(--ls-gold) 65%, var(--ls-line)); }
.label { color: var(--ls-gold); font-size: 1.15rem; font-weight: 800; }
.sub { color: var(--ls-muted); font-size: .9rem; margin-top: .2rem; }
</style>

<!-- Julien · 0:55 -->
